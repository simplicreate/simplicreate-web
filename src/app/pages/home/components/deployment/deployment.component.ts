import { Component } from '@angular/core';

@Component({
  selector: 'app-deployment',
  standalone: true,
  templateUrl: './deployment.component.html'
})
export class DeploymentComponent {
  // Purely static presentational component, no inputs needed right now!
}